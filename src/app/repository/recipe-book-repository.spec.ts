import { RecipeBookRepository } from './recipe-book-repository';

describe('RecipeBookRepository', () => {
  it('should create an instance', () => {
    expect(new RecipeBookRepository()).toBeTruthy();
  });
});
